import scoringBoardExtension from './boards/scoring_track_extension.jpg';

/** The Lost Fleet Scoring Board Extension, reverse side (3/4-player side, showing the 4
 * per-color Exploration Shuttle upgrade tracks) — this project is always 4 players, so the
 * front side (2-player "25 victory points" side) is never used and has no asset. */
export function scoringBoardExtensionImageSrc(): string {
  return scoringBoardExtension;
}
