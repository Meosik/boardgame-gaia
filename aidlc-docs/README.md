# Legacy AIDLC Documents

This directory describes the generated prototype and is retained only as historical/negative evidence.

It is **not** authoritative for the rebuild. In particular, references to AI coaching, MCTS, Qdrant, Ollama, legacy bidding, fictitious expansion factions, and older wire/database contracts are obsolete. Current decisions live in `.omx/specs` and `.omx/plans` and official game rules remain the product source of truth.
